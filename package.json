{
  "name": "comprar-iptv",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "mv wrangler.toml wrangler.bak && (astro build || (mv wrangler.bak wrangler.toml && exit 1)) && mv wrangler.bak wrangler.toml && rm -f node_modules/.bin/astro && printf '#!/bin/sh\\nexit 0\\n' > node_modules/.bin/astro && chmod +x node_modules/.bin/astro",
    "preview": "astro preview",
    "astro": "astro",
    "generate-types": "wrangler types"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.1.4",
    "@astrojs/sitemap": "^3.7.2",
    "@tailwindcss/vite": "^4.2.4",
    "astro": "^7.1.3",
    "lucide-astro": "^0.556.0",
    "tailwindcss": "^4.0.0",
    "wrangler": "^4.114.0"
  }
}
